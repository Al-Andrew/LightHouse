## Agent skills

### Issue tracker

GitHub hosts the repository and its issues. Before reading or updating
issues, read `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default triage labels. Before triaging issues, read
`docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context layout. Before exploring the codebase,
read `docs/agents/domain.md`.
